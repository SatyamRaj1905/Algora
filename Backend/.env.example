PORT=3000
FRONTEND_URL=http://localhost:5173
DB_CONNECT_STRING=your_mongodb_connection_string
JWT_KEY=your_secret_key
REDIS_PASS=your_redis_db_password

# Optional: only needed when the compiler/runtime is not available in PATH.
# CPP_COMPILER=/full/path/to/g++
# JAVA_COMPILER=/full/path/to/javac
# JAVA_RUNTIME=/full/path/to/java
